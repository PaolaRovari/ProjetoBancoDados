@model ProjetoBancoDados.Models.Lugar

@{
    ViewData["Title"] = "Details";
}

<h1>Detalhes</h1>

<div>
    <h4>Lugar</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NomeLugar)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NomeLugar)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.HorarioFuncionamento)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.HorarioFuncionamento)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.LugarId" class="btn btn-primary">Editar</a>
    <a asp-action="Index" class="btn btn-primary">Voltar à lista</a>
</div>
